#include <iostream>
#include <vector>
#include <cmath>
#include <stdio.h>
#include <typeinfo>
using namespace std;

int main()
{
    string X;
    int N;

    cin >> X;
    cin >> N;
    vector<string> S(N);
    for (int i = 0; i < S.size(); i++)
    {
        cin >> S[i];
    }
}